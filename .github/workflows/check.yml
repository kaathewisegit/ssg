name: Checks

on:
  push:
    branches: ["trunk"]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2

      - run: deno install --lock
      - run: deno task check
